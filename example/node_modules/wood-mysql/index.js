/**
 * Wood Plugin Module.
 * mysql操作库
 * by blucehuang on 2018-11-18
 */
const Mysql = require('./src/mysql');

module.exports = (app = {}, config = {}) => {
  app.Mysql = Mysql;
  if(app.addAppProp) app.addAppProp('Mysql', app.Mysql);
  return app;
}
