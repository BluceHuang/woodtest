/**
 * Wood Plugin Module.
 * mongo操作库
 * by blucehuang on 2018-11-18
 */
const Mysql = require('./src/mysql');

module.exports = async (app = {}, config = {}) => {
  for (let key in config) {
    await Mysql.connect(config[key], key);
  }
  return app;
}
